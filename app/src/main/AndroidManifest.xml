<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="algonquin.cst2335.hhsrandroidproject1">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.HHSRAndroidProject1">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MovieIfo.Movie_Database_API"/>
        <activity android:name=".MovieIfo.MovieInfoRecord"/>
        <activity android:name=".MovieIfo.SaveMovSecondActivity"/>
        <activity android:name=".MovieIfo.SavedMovies"/>



    </application>

</manifest>